<!DOCTYPE html>
<html>
<head>
  <title>Music Scholar:BETA</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <%= javascript_include_tag 'https://www.youtube.com/iframe_api' %>
</head>
<body>

<div class="player_nav">
	<% if player_signed_in? %>
		Signed in as <strong><%= current_player.email %></strong>. Not you?
|
		<%= link_to "Sign Out", destroy_player_session_path, :method => :delete %>
	<% else %>
		<%= link_to "Sign Up", new_player_registration_path %> or <%= link_to "Sign In", new_player_session_path %>
	<% end %>
</div>

<% flash.each do |name, msg| %>
<%= content_tag :div, msg, :id => "flash_#{name}" %>
<% end %>

<%= yield %>

</body>
</html>
